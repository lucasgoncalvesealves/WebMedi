package util;

//import model.clsEspecialidade;
//import dao.EspecialidadeDAO;

public class Teste {
	public static void main(String[] args) {
//		HibernateDAO dao = new HibernateDAO();
//		// Inclusão de novo item
//		
//		Hibernate hb = new Hibernate();
//		hb.setId(new BigDecimal(6));
//		hb.setDescricao("TESTE 2");
//		dao.salvar(hb);
		
		//Atualiza registros
		/*
		List<Hibernate> lista = dao.listaTodos();
		for(Hibernate h : lista) {
			System.out.println(h.getId() + " - " + h.getDescricao());
			h.setDescricao(h.getDescricao().concat(" teste"));
			dao.atualizar(h);
		}
		*/
		/*
		List<Hibernate> lista = dao.buscaPorNome("2");
		for(Hibernate h : lista) {
			System.out.println(h.getId() + " - " + h.getDescricao());

		}
		*/
		//EspecialidadeDAO dao1 = new EspecialidadeDAO();
		//clsEspecialidade TabEsp = new clsEspecialidade();
		//TabEsp.setDescricao("teste");
		//dao1.salvar(TabEsp);
		
			
	}
}
